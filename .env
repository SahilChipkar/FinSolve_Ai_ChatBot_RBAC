# Ollama Host URL
# If Ollama is running on the default port on your local machine, this is usually fine.
# Change if Ollama is hosted elsewhere or on a different port.
OLLAMA_HOST=http://localhost:11434

# Secret key for JWT authentication (generate a strong, random string!)
SECRET_KEY="your-super-secret-jwt-key-change-this-in-production"
ALGORITHM="HS256"

# Initial password for the 'admin' user created in src/database.py
# If not set, it defaults to "adminpass"
ADMIN_INITIAL_PASSWORD="adminpass" 